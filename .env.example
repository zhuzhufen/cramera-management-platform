# 数据库连接配置
# 使用 Docker Compose 时，DB_HOST 应为 postgres（容器名称）
DB_HOST=postgres
DB_PORT=5432
DB_NAME=camera_rental_management
DB_USER=postgres
DB_PASSWORD=123456

# 应用配置
NODE_ENV=production

# 密码加密配置
# 前端密码加密使用的密钥和盐值
# 生产环境请使用强随机密钥，不要使用默认值
ENCRYPTION_KEY=your_strong_encryption_key_here
ENCRYPTION_SALT=your_strong_encryption_salt_here
